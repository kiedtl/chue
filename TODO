TODO

- parser
	- ability to parse comon colors (e.g. "red", "blue", etc)
	- test/fix ability to parse hsv/hsl
- cleanse code of "Result" abomination
- manpage!
- `make install` recipe
- use `getopt_long` instead of `getopt` for parsing arguments
- release v0.1.0
